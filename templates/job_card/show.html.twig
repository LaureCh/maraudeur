{% extends 'base.html.twig' %}

{% block title %}JobCard{% endblock %}

{% block body %}
    <h1>JobCard</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_card.id }}</td>
            </tr>
            <tr>
                <th>GitLink</th>
                <td>{{ job_card.gitLink }}</td>
            </tr>
            <tr>
                <th>WebSiteLink</th>
                <td>{{ job_card.webSiteLink }}</td>
            </tr>
            <tr>
                <th>LinkedinLink</th>
                <td>{{ job_card.linkedinLink }}</td>
            </tr>
            <tr>
                <th>Cv</th>
                <td>{{ job_card.cv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('job_card_index') }}">back to list</a>

    <a href="{{ path('job_card_edit', {'id': job_card.id}) }}">edit</a>

    {{ include('job_card/_delete_form.html.twig') }}
{% endblock %}
